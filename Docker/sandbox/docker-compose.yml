version: "3.9"

services:
  sandbox:
    build: .
    container_name: python_sandbox
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules

    command: npm run dev

    restart: unless-stopped